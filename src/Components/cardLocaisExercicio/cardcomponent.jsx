

const cardcomponents = ({props}) => {

            return(

                <div>
                    <h1>{props.nomeLocal}</h1>
                    <h3>{props.nomeLocal}</h3>
                </div>

            )
}